apiVersion: v1
kind: Service
metadata:
  name: svc-app
  namespace: jenkins
  labels:
    app: service-rest
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: service-rest
  type: NodePort
