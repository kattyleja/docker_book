apiVersion: v1
kind: Service
metadata:
  name: svc-db
  namespace: docker-book
  labels:
    app: service-rest
spec:
  ports:
  - port: 8080
    name: http
  selector:
    app: service-rest
  type: NodePort
